import 'package:flutter/material.dart';

class Palette {
// Background color
  static const Color bg = Color.fromRGBO(50, 50, 50, 1);

// Font color
  static const Color font = Colors.white;
  static const Color font_light = Color.fromRGBO(216, 209, 209, 1);

// Accent colors
  static const Color accent_orange = Color.fromRGBO(235, 150, 110, 1);
  static const Color accent_yellow = Color.fromRGBO(214, 196, 151, 1);
  static const Color accent_blue = Color.fromRGBO(150, 204, 214, 1);
  static const Color accent_pink = Color.fromRGBO(219, 178, 161, 1);

// Quiz buttons
  static const Color button_next = Color.fromRGBO(149, 211, 144, 1);
  static const Color button_skip = Color.fromRGBO(211, 192, 144, 1);
}
